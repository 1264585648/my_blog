---
import SectionTitle from './SectionTitle.astro';
import { learningPath } from '../data/home';
import { withBase } from '../utils/paths';
---
<section class="section shell learning-section">
  <SectionTitle title="Learning" href="/learning/" linkText="学习地图 →" />
  <div class="learning-list">
    {learningPath.map((step) => (
      <a class="learning-row" href={withBase('/learning/')}>
        <span class="step-index">{step.index}</span>
        <span class="step-copy">
          <strong>{step.title}</strong>
          <small>{step.description}</small>
        </span>
        <span class="learning-status">持续整理 <b>↗</b></span>
      </a>
    ))}
  </div>
</section>
