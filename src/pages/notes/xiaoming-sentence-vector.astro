---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { withBase } from '../../utils/paths';
---

<BaseLayout
  title="「小明打了小红」是怎么变成一个向量的？ · xuan.z 的个人笔记"
  description="一篇碎片笔记：用「小明打了小红」这个最小例子，理解 Token、上下文表示、Transformer 和 Pooling 是怎么一起得到 Sentence Vector 的。"
>
  <article class="note-page shell">
    <a class="back-link" href={withBase('/')}>← 返回首页</a>

    <header class="note-hero">
      <p class="note-kicker">碎片笔记 · LLM / Embedding</p>
      <h1>「小明打了小红」是怎么变成一个向量的？</h1>
      <p class="subtitle">我真正想弄明白的不是“Embedding 是什么”，而是一个更具体的问题：一整句话最后为什么只会得到一个 Vector？这个 Vector 又不是把几个词简单加起来，那中间到底发生了什么？</p>
      <div class="meta"><span>2026-08-12</span><span>约 10 分钟</span><span>一个问题，一次推导</span></div>
    </header>

    <section class="content-section first">
      <p class="section-label">问题从这里出现</p>
      <h2>RAG 里为什么“一整段文字 → 一个 Vector”？</h2>
      <p>之前理解 Embedding 时，我已经知道模型会先把文字拆成 Token。比如一句：</p>
      <div class="sentence-card">HashMap 底层使用数组和链表 / 红黑树</div>
      <p>内部并不是整句话直接跳进一个神秘的“向量化函数”，而是仍然会先经过 Tokenizer，得到多个 Token。</p>
      <p>但 RAG 教程里经常把中间过程简写成：</p>
      <pre><code>一段文字
   ↓
Embedding Model
   ↓
[0.21, -0.51, 0.82, ...]</code></pre>
      <p>于是我产生了一个很具体的疑问：</p>
      <div class="question-card">
        <span>我的疑问</span>
        <strong>既然内部明明有很多 Token，为什么最后可以只得到一个代表整句话的 Vector？</strong>
      </div>
      <p>为了把这个问题看清楚，HashMap 这个例子反而不够用了。因为它不容易暴露“词之间的关系”到底有没有被保留下来。</p>
    </section>

    <section class="content-section">
      <p class="section-label">换一个更容易暴露问题的例子</p>
      <h2>「小明打了小红」和「小红打了小明」</h2>
      <div class="contrast-grid">
        <div><small>句子 A</small><strong>小明打了小红</strong></div>
        <div><small>句子 B</small><strong>小红打了小明</strong></div>
      </div>
      <p>这两句话包含的 Token 几乎一样，但人一眼就知道它们表达的事情完全不同。</p>
      <p>这说明最终的 Sentence Vector 不能只记录：</p>
      <pre><code>这里出现过：小明、打了、小红</code></pre>
      <p>它还必须 somehow 保留：</p>
      <pre><code>谁在前？
谁做了动作？
谁是动作的对象？
这些 Token 在当前句子里是什么关系？</code></pre>
      <p>所以真正的问题从“怎么把几个词变成一个向量”变成了：</p>
      <blockquote>模型怎样先让每个 Token 获得上下文信息，再把整句话压成一个 Vector？</blockquote>
    </section>

    <section class="content-section">
      <p class="section-label">一步步走完整个过程</p>
      <h2>第一步：先保持三个 Token，而不是急着合成一个值</h2>
      <p>为了方便理解，先假设它刚好被拆成三个 Token：</p>
      <div class="token-row"><span>小明</span><span>打了</span><span>小红</span></div>
      <p>每个 Token 先得到自己的初始向量：</p>
      <div class="token-row vectors"><span>小明<small>Vector A</small></span><span>打了<small>Vector B</small></span><span>小红<small>Vector C</small></span></div>
      <p>到这里仍然是 <strong>3 个向量</strong>，并没有出现所谓的“句子向量”。</p>

      <h2>第二步：加入位置信息</h2>
      <p>如果没有位置，“小明 / 打了 / 小红”和“小红 / 打了 / 小明”只看 Token 集合会非常接近，所以模型还需要知道谁在第 1、2、3 个位置。</p>
      <div class="token-row position"><span>A + 位置 1</span><span>B + 位置 2</span><span>C + 位置 3</span></div>

      <h2>第三步：Transformer 让三个位置互相获取信息</h2>
      <p>接下来最关键的事情发生了。三个位置并不是从左往右依次相加，而是在 Transformer 中彼此参考。</p>
      <div class="context-flow">
        <div class="before"><b>进入前</b><span>小明 · A</span><span>打了 · B</span><span>小红 · C</span></div>
        <div class="center"><strong>Transformer / Attention</strong><p>每个位置根据当前内容和模型参数，决定应该从其他位置吸收多少信息。</p></div>
        <div class="after"><b>出来后</b><span>小明位置 · h₁</span><span>打了位置 · h₂</span><span>小红位置 · h₃</span></div>
      </div>
      <p>这一步之后仍然不是一个向量，而是三个新的向量：</p>
      <pre><code>h₁  → “小明”这个位置的上下文化表示
h₂  → “打了”这个位置的上下文化表示
h₃  → “小红”这个位置的上下文化表示</code></pre>

      <div class="correction-card">
        <span>这里我之前说得不严谨</span>
        <strong>不是“小明这个词的含义变了”，而是“小明这个位置的表示”融入了当前句子的上下文和关系信息。</strong>
      </div>

      <p>而且这个过程不是只做一轮。真实模型会经过多层 Transformer：</p>
      <pre><code>A   B   C
↓   ↓   ↓
Layer 1
↓   ↓   ↓
h1  h2  h3
↓   ↓   ↓
Layer 2
↓   ↓   ↓
h1' h2' h3'
...
↓
更充分的上下文化表示</code></pre>
      <p>我现在更愿意把它理解成：<strong>三个位置开了很多轮信息交换会议</strong>。每一轮都还是三个位置，但它们越来越知道自己在整句话里的关系。</p>

      <figure class="image-placeholder">
        <div class="placeholder-art">IMAGE · 01</div>
        <figcaption><strong>待生成配图：三个 Token 如何被上下文化</strong><span>后续生成后替换。</span></figcaption>
        <details>
          <summary>查看配图提示词</summary>
          <p>16:9 中文技术学习信息图，标题“「小明打了小红」在 Transformer 里发生了什么？”。第一行并列三个 Token：小明、打了、小红；第二行分别是初始向量 A/B/C；第三行加入位置 1/2/3；中间是多层 Transformer / Attention，用网状细线表示三个位置互相读取信息，不要画成顺序相加；输出 h1/h2/h3，并明确标注“仍然是 3 个向量，但每个都已融入上下文”。右侧放一句提醒：“不是词义变了，而是这个位置的表示被上下文化”。风格匹配温暖技术博客：奶油白、鼠尾草绿、雾蓝、少量橙色，简洁准确。</p>
        </details>
      </figure>
    </section>

    <section class="content-section">
      <p class="section-label">我当时最大的误解</p>
      <h2>不是“小明 + 打了 → X，再 X + 小红”</h2>
      <p>理解到这里以后，我最自然的猜测其实是：</p>
      <div class="wrong-flow"><span>小明</span><b>+</b><span>打了</span><i>→</i><span>中间值 X</span><b>+</b><span>小红</span><i>→</i><strong>最终 Vector</strong><em>✕</em></div>
      <p>这种想法把模型理解成了一个从左到右不断累加的计算器，但更接近真实过程的是：</p>
      <div class="correct-flow">
        <div><span>小明</span><small>A</small></div>
        <div><span>打了</span><small>B</small></div>
        <div><span>小红</span><small>C</small></div>
        <b>↓</b>
        <strong>多层 Transformer：三个位置反复交换上下文信息</strong>
        <b>↓</b>
        <div><span>h₁</span><small>上下文化位置 1</small></div>
        <div><span>h₂</span><small>上下文化位置 2</small></div>
        <div><span>h₃</span><small>上下文化位置 3</small></div>
      </div>
      <p>也就是说，<strong>“合成一个最终 Vector”并不是 Transformer 从左到右做出来的。</strong> Transformer 的主要结果仍然是多个位置各自的 Hidden State。</p>
    </section>

    <section class="content-section">
      <p class="section-label">最后一步才真正得到一句话的 Vector</p>
      <h2>Pooling：把多个上下文化表示汇总起来</h2>
      <p>如果这是一个用于 RAG 检索的 Embedding Model，我们最终需要的不是 h₁、h₂、h₃ 三个结果，而是一个代表整句话的 Vector。</p>
      <div class="pooling-flow">
        <span>h₁</span><span>h₂</span><span>h₃</span><i>→</i><strong>Pooling</strong><i>→</i><b>[0.31, -0.72, 0.48, ...]</b>
      </div>
      <p>最容易理解的一种方式是 Mean Pooling：把多个 Token 的上下文化向量按维度取平均。实际模型也可能使用 CLS、Last Token 或专门训练的汇总方式。</p>
      <p>但这里真正应该记住的不是 Pooling 有多少种，而是这条因果链：</p>
      <blockquote><strong>整段向量化 ≠ 不分 Token。</strong><br />它其实是：先拆成 Token → 每个 Token 得到初始表示 → Transformer 建模上下文 → 得到多个上下文化表示 → 最后 Pooling 成一个 Sentence / Chunk Vector。</blockquote>

      <figure class="image-placeholder compact">
        <div class="placeholder-art">IMAGE · 02</div>
        <figcaption><strong>待生成配图：一句话如何从 3 个 Token 走到 1 个 Vector</strong><span>重点画出“多个 → 上下文化 → 汇总”。</span></figcaption>
        <details>
          <summary>查看配图提示词</summary>
          <p>横版中文教学图，标题“一句话为什么最后只有一个 Vector？”。从左到右：文本“小明打了小红” → Tokenizer → [小明][打了][小红] → 三个初始 Vector A/B/C → 多层 Transformer 形成 h1/h2/h3 → Pooling → 一个 Sentence Vector。中间用醒目标注“不是 A+B+C 的简单求和，也不是从左到右累加”。底部总结：“先分别表示，先理解上下文，最后汇总”。风格与个人博客一致，奶油白、鼠尾草绿、雾蓝，清晰留白。</p>
        </details>
      </figure>
    </section>

    <section class="summary-card">
      <p class="section-label">这次我最终记住的东西</p>
      <h2>一句话版</h2>
      <p><strong>「小明打了小红」不是“小明 + 打了 + 小红”算出来一个值。</strong>三个 Token 会先各自成为向量，在 Transformer 中不断吸收彼此的上下文信息，得到多个 Hidden State，最后 Embedding Model 再通过 Pooling 等方式把它们汇总成一个 Sentence Vector。</p>
      <div class="memory-line">Token → 初始 Vector → Contextual Hidden States → Pooling → Sentence Vector</div>
    </section>

    <section class="open-questions">
      <p class="section-label">下一次继续追的问题</p>
      <h2>还有两处我现在只是“先接受”</h2>
      <p>这篇只把主链串起来，还没有继续拆两个更底层的问题：</p>
      <ul>
        <li><strong>Attention 到底怎么计算“该关注谁”？</strong> —— 这会进入 Q、K、V。</li>
        <li><strong>Pooling 压成一个 Vector 为什么还能用于语义检索？</strong> —— 这会继续连接 Embedding 模型的训练目标和相似度计算。</li>
      </ul>
    </section>
  </article>
</BaseLayout>

<style>
  .note-page { max-width: 860px; padding-block: 42px 82px; }
  .back-link { display: inline-block; margin-bottom: 22px; color: var(--ink-faint); font-size: 12px; }
  .note-hero { padding: 34px 36px; border: 1px solid var(--line); border-radius: 22px; background: rgba(255,253,248,.76); box-shadow: var(--shadow); }
  .note-kicker, .section-label { margin: 0; color: var(--sage); font-size: 10px; font-weight: 800; letter-spacing: .14em; }
  h1 { margin: 10px 0 14px; font-family: var(--hand-font); font-size: clamp(36px, 5vw, 54px); line-height: 1.2; }
  .subtitle { margin: 0; color: var(--ink-soft); font-family: var(--body-font); line-height: 1.9; }
  .meta { display: flex; flex-wrap: wrap; gap: 16px; margin-top: 18px; color: var(--ink-faint); font-size: 11px; }
  .content-section { padding: 44px 6px 0; }
  .content-section.first { padding-top: 36px; }
  .content-section h2, .summary-card h2, .open-questions h2 { margin: 8px 0 14px; font-family: var(--hand-font); font-size: 29px; }
  .content-section > p, .summary-card > p, .open-questions > p, .open-questions li { color: #5d594f; font-family: var(--body-font); font-size: 15px; line-height: 1.95; }
  pre { overflow-x: auto; margin: 18px 0; padding: 19px 21px; border: 1px solid var(--line); border-radius: 14px; background: #f8f6ef; color: #514e47; font-size: 13px; line-height: 1.75; }
  blockquote { margin: 20px 0; padding: 16px 20px; border-left: 3px solid var(--sage); background: rgba(239,243,234,.72); color: #55594f; line-height: 1.85; }
  .sentence-card { margin: 18px 0; padding: 18px 20px; border: 1px solid #cbd6c2; border-radius: 13px; background: #f5f8f1; color: #53594e; font-weight: 700; text-align: center; }
  .question-card, .correction-card { display: grid; gap: 7px; margin: 20px 0; padding: 18px 20px; border: 1px solid #d8c9af; border-radius: 14px; background: #fff8eb; }
  .question-card span, .correction-card span { color: #a78251; font-size: 10px; font-weight: 800; letter-spacing: .12em; }
  .question-card strong, .correction-card strong { color: #5e5548; line-height: 1.75; }
  .correction-card { border-color: #d9c2aa; background: #fff5eb; }
  .contrast-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 14px; margin: 18px 0; }
  .contrast-grid div { display: grid; gap: 7px; padding: 18px; border: 1px solid #cbd6c2; border-radius: 14px; background: #f6f8f2; text-align: center; }
  .contrast-grid small { color: var(--sage); font-weight: 800; }
  .contrast-grid strong { color: #52554d; font-size: 18px; }
  .token-row { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; margin: 18px 0; }
  .token-row span { display: flex; flex-direction: column; gap: 6px; align-items: center; padding: 14px; border: 1px solid #cad5c2; border-radius: 12px; background: #f5f8f1; color: #51574c; font-weight: 700; }
  .token-row small { color: var(--ink-faint); font-weight: 500; }
  .vectors span { background: #f8f6ef; }
  .position span { background: #eef3e9; }
  .context-flow { display: grid; grid-template-columns: 1fr 1.25fr 1fr; gap: 12px; margin: 20px 0; }
  .context-flow > div { padding: 17px; border: 1px solid var(--line); border-radius: 14px; }
  .context-flow .before, .context-flow .after { display: grid; gap: 8px; background: #faf8f2; }
  .context-flow .before span, .context-flow .after span { padding: 7px 9px; border-radius: 8px; background: #fffdf8; font-size: 12px; }
  .context-flow .center { background: #eef4ea; }
  .context-flow .center p { color: #5b6155; font-size: 12px; line-height: 1.7; }
  .context-flow .after { background: #f2f7ee; }
  .wrong-flow { display: flex; flex-wrap: wrap; gap: 9px; align-items: center; margin: 18px 0; padding: 18px; border: 1px solid #e0c5bd; border-radius: 14px; background: #fbf3ef; color: #704f47; }
  .wrong-flow span, .wrong-flow strong { padding: 8px 11px; border: 1px solid #dfbdb3; border-radius: 9px; background: #fffaf7; }
  .wrong-flow b, .wrong-flow i, .wrong-flow em { color: #b86d5c; font-style: normal; }
  .correct-flow { display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px; margin: 20px 0; }
  .correct-flow div { display: flex; flex-direction: column; gap: 5px; padding: 14px; border: 1px solid #c9d5c1; border-radius: 12px; background: #f5f8f1; text-align: center; }
  .correct-flow > b, .correct-flow > strong { grid-column: 1/-1; color: #64705e; text-align: center; }
  .correct-flow small { color: var(--ink-faint); }
  .pooling-flow { display: flex; flex-wrap: wrap; gap: 9px; align-items: center; justify-content: center; margin: 20px 0; padding: 18px; border: 1px solid #cad6c2; border-radius: 14px; background: #f5f8f1; }
  .pooling-flow span, .pooling-flow strong, .pooling-flow b { padding: 9px 11px; border-radius: 9px; background: #fffdf8; }
  .pooling-flow i { color: var(--sage); font-style: normal; }
  .pooling-flow b { color: #56604f; }
  .image-placeholder { margin: 26px 0 4px; padding: 15px; border: 1px dashed #bfcbb7; border-radius: 16px; background: linear-gradient(145deg, #f4f7ef, #fbf9f3); }
  .image-placeholder.compact .placeholder-art { min-height: 170px; }
  .placeholder-art { display: grid; min-height: 220px; place-items: center; border: 1px solid #d9dfd3; border-radius: 12px; background: repeating-linear-gradient(135deg,#f8f7f1,#f8f7f1 14px,#f3f4ee 14px,#f3f4ee 28px); color: #98a090; font-size: 12px; font-weight: 800; letter-spacing: .16em; }
  .image-placeholder figcaption { display: flex; flex-direction: column; gap: 4px; padding: 11px 4px 3px; color: #5f6259; font-size: 12px; }
  .image-placeholder figcaption span { color: var(--ink-faint); }
  .image-placeholder details { margin-top: 8px; padding: 10px 12px; border-radius: 10px; background: rgba(255,253,248,.7); }
  .image-placeholder summary { cursor: pointer; color: var(--sage-dark); font-size: 11px; font-weight: 700; }
  .image-placeholder details p { color: #69655c; font-size: 12px; line-height: 1.75; }
  .summary-card, .open-questions { margin-top: 38px; padding: 25px 28px; border: 1px solid #cbd6c2; border-radius: 18px; background: linear-gradient(145deg,#eef3e9,#f8f7f1); }
  .memory-line { margin-top: 18px; padding: 13px 15px; border-radius: 11px; background: rgba(255,253,248,.78); color: #596052; font-weight: 700; text-align: center; }
  .open-questions { background: rgba(255,253,248,.72); }
  .open-questions ul { margin: 12px 0 0; padding-left: 21px; }
  @media (max-width: 720px) {
    .note-page { width: min(100% - 28px, 860px); padding-top: 24px; }
    .note-hero { padding: 26px 22px; }
    .contrast-grid, .token-row, .context-flow, .correct-flow { grid-template-columns: 1fr; }
  }
</style>