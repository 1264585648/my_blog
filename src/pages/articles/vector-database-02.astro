---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ArticleToc from '../../components/ArticleToc.astro';
import { withBase } from '../../utils/paths';

const tocItems = [
  { id: 'origin', label: '这篇笔记是怎么来的？', hint: '从 Parameter 与 Token 的疑问开始' },
  { id: 'token-parameter', label: 'Token 本身是 Parameter 吗？', hint: '先把输入、编号和参数拆开' },
  { id: 'embedding-table', label: 'Embedding 到底是谁生成谁？', hint: '训练阶段与推理阶段' },
  { id: 'rag-turn', label: '为什么我突然想到 RAG？', hint: '从 Token Vector 迁移到 Chunk Vector' },
  { id: 'context', label: '小明打了小红：上下文怎么进入向量？', hint: '用反例逼出真正的问题' },
  { id: 'sentence-vector', label: '一句话是怎么得到最终 Vector 的？', hint: '不是从左到右依次相加' },
  { id: 'migration', label: '换 Embedding 模型会发生什么？', hint: '迁移后果与工程成本' },
  { id: 'review', label: '我现在形成了什么理解？', hint: '把整条认知链重新串起来' },
  { id: 'next', label: '还有哪些问题没解决？', hint: '把疑问留给下一篇' }
];
---

<BaseLayout
  title="02｜从 Token 到 Vector：一句话到底是怎么被模型变成向量的？ · xuan.z 的个人笔记"
  description="一篇按真实学习顺序整理的笔记：从 Token、Parameter 的疑问出发，经由 Embedding Table、RAG、上下文表示和 Pooling，最终理解一句话为什么能变成一个可检索的 Vector。"
>
  <article class="article-page shell">
    <a class="back-link" href={withBase('/series/vector-database/')}>← 返回 Vector Database 系列</a>

    <header class="article-hero">
      <div>
        <a class="series series-link" href={withBase('/series/vector-database/')}>BUILD YOUR OWN VECTOR DATABASE · 02 ↗</a>
        <h1>从 Token 到 Vector：一句话到底是怎么被模型变成向量的？</h1>
        <p class="subtitle">这不是一篇按概念顺序写的教材，而是一次真实的学习记录：我先卡在 Parameter 怎么和 Token 计算，后来一路追问到 Embedding、RAG、上下文表示和 Pooling，才终于把“整段文字为什么能变成一个 Vector”串起来。</p>
        <div class="meta"><span>2026-08-12</span><span>约 25 分钟</span><span>学习过程记录</span></div>
      </div>
      <div class="hero-flow" aria-label="这次学习的认知推进">
        <span>Parameter × Token？</span><i>↓</i><span>Token → Embedding</span><i>↓</i><span>RAG 为什么整段向量化？</span><i>↓</i><span>上下文怎么进入 Vector？</span><i>↓</i><span>Sentence / Chunk Vector</span>
      </div>
    </header>

    <ArticleToc items={tocItems} />

    <section class="goal-card">
      <p class="section-label">这篇笔记想解决什么</p>
      <p>我最初并不是为了“系统学习 Embedding”而开始这一段。真正的起点，是在理解大模型 Parameter 时遇到一个很具体的问题：<strong>Token 到底怎么和 Parameter 发生计算？</strong></p>
      <p>随着这个问题往下追，我又连续冒出了几个新的疑惑。它们看起来分散，其实最终都指向同一个核心：<strong>文字进入模型以后，到底经历了什么，才变成一个可以参与计算、甚至可以拿去做 RAG 检索的 Vector？</strong></p>
      <div class="question-chain">
        <span>Token 是 Parameter 吗？</span><i>→</i>
        <span>Embedding 表里的数字是谁训练出来的？</span><i>→</i>
        <span>RAG 为什么能把整段话变成一个向量？</span><i>→</i>
        <span>上下文关系怎么进入向量？</span><i>→</i>
        <span>换 Embedding 模型为什么要重建？</span>
      </div>
    </section>

    <section id="origin" class="content-section">
      <p class="section-label">01 · 起因</p>
      <h2>一开始，我只是想弄懂 Parameter 到底在干什么</h2>
      <p>前面学习大模型部署和微调时，我碰到了 <strong>Parameter、Token、Embedding、Attention</strong> 这些词。Parameter 的定义并不难理解：它就是模型训练后保存下来的大量数字。但定义懂了以后，一个更实际的问题出现了：</p>

      <div class="question-card">
        <span>最开始的问题</span>
        <strong>Parameter 这些数字，到底是怎么和 Token 发生计算的？</strong>
      </div>

      <p>为了回答这个问题，我先尝试从最小链路去理解：</p>
      <div class="mini-flow"><span>文字</span><i>→</i><span>Token</span><i>→</i><span>Token ID</span><i>→</i><span>Vector</span><i>→</i><span>Parameter 参与计算</span></div>
      <p>这里第一次出现了一个关键转折：<strong>模型并不是直接拿“文字”去和 Parameter 计算。Token 必须先变成数字表示。</strong></p>

      <h3>最初用的例子：猫</h3>
      <pre><code>“猫”
  ↓ Tokenizer
Token：猫
  ↓
Token ID：1357
  ↓ 查 Embedding Table
[0.82, 0.31, -0.17, ...]</code></pre>
      <p>到这里我以为自己已经理解了：Token 查表得到 Vector，然后 Parameter 再去加工这个 Vector。</p>
      <p>但这个例子马上引出了第一个真正的疑惑。</p>
    </section>

    <section id="token-parameter" class="content-section">
      <p class="section-label">02 · 第一个疑惑</p>
      <h2>既然 Token 最后也会变成数字，它本身是不是 Parameter？</h2>

      <div class="question-card">
        <span>当时的疑问</span>
        <strong>“猫”最后对应 [0.82, 0.31, -0.17, ...]，这些都是数字，那 Token 本身是不是也属于 Parameter？</strong>
      </div>

      <p>这个问题逼着我第一次把几个很容易混在一起的东西真正拆开：</p>
      <div class="concept-grid three">
        <div><b>Token</b><code>“猫”</code><p>本次输入被切出来的文本单位，是数据。</p></div>
        <div><b>Token ID</b><code>1357</code><p>Token 在词表中的编号。它只回答“你是谁”。</p></div>
        <div class="focus"><b>Parameter</b><code>0.82, -0.17, ...</code><p>训练后长期保存在模型中的数字，是模型的一部分。</p></div>
      </div>

      <p>真正容易混淆的是：Embedding Table 本身就是模型参数的一部分。例如：</p>
      <pre><code>Embedding Table

ID 1356 → [ 0.21, -0.14,  0.65, ... ]
ID 1357 → [ 0.82,  0.31, -0.17, ... ]  ← “猫”
ID 1358 → [-0.11,  0.72,  0.23, ... ]</code></pre>
      <p>当输入里出现“猫”时，模型只是根据 ID=1357，把表里的这一行取出来，作为这个 Token 的<strong>初始数字表示</strong>。</p>

      <blockquote>这一步我形成的第一个稳定认识：<strong>Token 是原料；Token ID 是编号；Parameter 是模型里训练好的数字。</strong></blockquote>

      <p>但是新的问题马上又来了：如果 Embedding Table 里的那些数字本身就是 Parameter，为什么又说“Embedding 把 Token 变成向量”？这些数字到底是现场算出来的，还是以前就已经存在？</p>
    </section>

    <section id="embedding-table" class="content-section">
      <p class="section-label">03 · 第二个疑惑</p>
      <h2>Embedding 表里的数字不是 Embedding 出来的吗？为什么又说它们是训练出来的？</h2>

      <div class="question-card">
        <span>当时的疑问</span>
        <strong>这些数字不是 Embedding 出来的吗？难道“训练模型”就是在训练 Embedding？</strong>
      </div>

      <p>这里真正让我混乱的是 <strong>Embedding 这个词被同时用在两个不同层面</strong>。</p>
      <div class="concept-grid two">
        <div><b>Embedding Table / Layer</b><p>模型内部的一张参数表。表里的数字属于 Parameter，是训练过程中逐渐学出来的。</p></div>
        <div><b>Embedding Vector</b><p>推理时，根据 Token ID 从这张表中取出来的一行数字，用作这个 Token 的初始表示。</p></div>
      </div>

      <h3>为了想明白，我把“训练”和“推理”分开看</h3>
      <div class="phase-compare">
        <div>
          <small>训练阶段</small>
          <h3>模型在调整 Parameter</h3>
          <pre><code>大量训练文本
   ↓
预测下一个 Token
   ↓
和正确答案比较
   ↓
产生 Loss
   ↓
反向传播
   ↓
Embedding / Attention / MLP ...
大量 Parameter 一起被调整</code></pre>
        </div>
        <div>
          <small>推理阶段</small>
          <h3>模型在使用已经训练好的 Parameter</h3>
          <pre><code>Token “猫”
   ↓
ID = 1357
   ↓
查 Embedding Table
   ↓
得到第 1357 行
[0.82, 0.31, -0.17, ...]</code></pre>
        </div>
      </div>

      <p>因此，“Embedding 出一个 Vector”这个说法在推理阶段很容易让人误以为那些数字是临时生成的。更准确的说法应该是：<strong>Token ID 先从已经训练好的 Embedding 参数中取得初始表示，后面 Transformer 再继续加工。</strong></p>
      <p class="note">训练也绝不只是训练 Embedding。Embedding Table 只是整个模型 Parameter 的一部分，Attention、MLP 等模块里还有大量参数会一起学习。</p>

      <figure class="image-placeholder compact">
        <div class="placeholder-art">IMAGE · 01</div>
        <figcaption><strong>待生成配图：训练阶段 VS 推理阶段</strong><span>这张图重点解释“训练 = 改参数；推理 = 用参数”。</span></figcaption>
        <details>
          <summary>查看配图提示词</summary>
          <p>16:9 横版中文技术学习信息图，风格匹配温暖纸张感个人技术博客：奶油白背景、鼠尾草绿、雾蓝、少量橙色。标题“Embedding Table 的数字到底从哪里来？”。左侧“训练阶段”：大量文本 → 预测下一个 Token → Loss → 反向传播 → Embedding Table、Attention、MLP 参数一起被调整，突出“数字逐渐被学出来”。右侧“推理阶段”：Token「猫」→ ID 1357 → 查已经训练好的 Embedding Table 第 1357 行 → 得到初始 Token Vector。底部大字总结：“训练 = 学 Parameter；推理 = 使用 Parameter”。避免复杂公式。</p>
        </details>
      </figure>
    </section>

    <section id="rag-turn" class="content-section">
      <p class="section-label">04 · 一个意外的联想</p>
      <h2>理解到这里，我突然想到：那 RAG 的“向量化”又是什么？</h2>

      <p>到这里，我对单个 Token 的链路已经有了一个基本认识：</p>
      <div class="mini-flow"><span>Token</span><i>→</i><span>Token ID</span><i>→</i><span>初始 Token Vector</span></div>

      <p>但我之前做 RAG 时看到的描述通常不是“每个 Token 一个 Vector”，而是：</p>
      <pre><code>“HashMap 底层使用数组和链表 / 红黑树”
                    ↓
             Embedding Model
                    ↓
       [0.21, -0.51, 0.82, ...]</code></pre>
      <p>也就是说，教程看起来像是在说：<strong>一整句话，甚至一个几百 Token 的 Chunk，最后只会得到一个 Vector。</strong></p>

      <div class="question-card">
        <span>新的疑问</span>
        <strong>模型不是先把文本分成 Token 吗？为什么做 RAG 时，却可以直接把“HashMap 底层使用数组和链表 / 红黑树”整段向量化？它难道没有经过 Token？</strong>
      </div>

      <p>这个问题非常关键，因为它把我前面理解的“Token Embedding”和 RAG 里的“Text Embedding”连接了起来。</p>
      <p>答案并不是“RAG 跳过 Token”，而是：</p>
      <blockquote><strong>整段向量化，内部仍然要先 Tokenize。</strong>区别只是 Embedding 模型在处理完所有 Token 后，还会进一步把整段上下文汇总成一个 Sentence / Chunk Vector。</blockquote>

      <p>不过说到这里，又出现了一个不能被忽略的问题：如果只是把每个 Token 的向量简单平均，那“谁做了什么”这种关系会不会丢掉？为了验证这个问题，我换了一个更容易看出语序差异的例子。</p>
    </section>

    <section id="context" class="content-section">
      <p class="section-label">05 · 用反例逼出真正的问题</p>
      <h2>为什么我要改用「小明打了小红」这个例子？</h2>

      <p>最初用“HashMap 底层使用数组和链表 / 红黑树”可以说明“整段文本最后会得到一个 Vector”，但它不够适合解释<strong>上下文关系为什么重要</strong>。</p>
      <p>于是我换成了两句话：</p>
      <div class="contrast-sentences">
        <div><small>句子 A</small><strong>小明打了小红</strong></div>
        <div><small>句子 B</small><strong>小红打了小明</strong></div>
      </div>
      <p>这两句话包含几乎完全相同的 Token，但人一眼就知道它们表达的是两件不同的事。如果模型只是拿“小明、打了、小红”三个孤立 Vector 做无脑汇总，那么 A 和 B 很容易变得过于相似。</p>

      <p>这时问题就变得清晰了：</p>
      <div class="question-card">
        <span>真正需要解决的问题</span>
        <strong>Vector 里不仅要包含“有哪些 Token”，还必须 somehow 包含“它们在当前上下文中是什么关系”。</strong>
      </div>

      <h3>第一步：每个 Token 先有自己的初始表示</h3>
      <div class="token-row"><span>小明<small>A</small></span><span>打了<small>B</small></span><span>小红<small>C</small></span></div>

      <h3>第二步：模型还要知道顺序</h3>
      <div class="position-flow"><span>A + 位置 1</span><span>B + 位置 2</span><span>C + 位置 3</span></div>
      <p>这样至少能区分“谁在前、谁在后”。</p>

      <h3>第三步：Attention 让每个位置吸收其他位置的信息</h3>
      <div class="attention-demo">
        <div class="source"><b>初始</b><span>小明 · A</span><span>打了 · B</span><span>小红 · C</span></div>
        <div class="meeting"><strong>Attention / Transformer</strong><p>三个位置不是从左到右顺次相加，而是在同一层里根据模型参数计算彼此的相关程度，并把需要的信息融入自己的表示。</p></div>
        <div class="source result"><b>更新后</b><span>小明位置 · h₁</span><span>打了位置 · h₂</span><span>小红位置 · h₃</span></div>
      </div>

      <p>这时我曾经用过一句不够严谨的话：“小明的含义发生了变化”。仔细想以后，这种表述会让人以为 Token 本身的词义被改掉了。</p>
      <div class="question-card caution">
        <span>一次重要纠正</span>
        <strong>不是“小明这个词的含义变了”，而是「小明这个 Token 位置的表示」在经过上下文建模后，融入了当前句子中的关系信息。</strong>
      </div>
      <p>更准确地说，<code>h₁</code> 是“小明”这个位置的 <strong>Contextualized Representation / Hidden State</strong>。它仍然对应“小明”这个位置，但它已经不再只是最初查 Embedding Table 得到的静态初始向量。</p>

      <h3>而且这个更新不是只做一次</h3>
      <pre><code>A    B    C
↓    ↓    ↓
Transformer Layer 1
↓    ↓    ↓
h1   h2   h3
↓    ↓    ↓
Transformer Layer 2
↓    ↓    ↓
h1'  h2'  h3'
...
↓
更深层的上下文化表示</code></pre>
      <p>我现在更愿意把它理解成：<strong>三个位置开了很多轮“信息交换会议”</strong>。每一轮都保留三个位置，但每个位置携带的上下文信息越来越丰富。</p>

      <figure class="image-placeholder">
        <div class="placeholder-art">IMAGE · 02</div>
        <figcaption><strong>待生成配图：「小明打了小红」为什么不能简单相加</strong><span>全文最重要的一张原理图。</span></figcaption>
        <details>
          <summary>查看配图提示词</summary>
          <p>16:9 中文技术学习信息图，标题“「小明打了小红」是怎样形成上下文表示的？”。第一行三个并列 Token：小明、打了、小红；第二行各自 Embedding Vector A/B/C；第三行加入位置 1/2/3；中间画多层 Transformer / Attention，用网状细线表现三个位置之间互相读取信息，严禁画成“小明 + 打了 → X，再 + 小红”的顺序累加；输出 h1/h2/h3，并标注“仍然是 3 个位置，但每个位置都已融入上下文”。右侧对比句子“小明打了小红 / 小红打了小明”，强调 Token 几乎相同但关系不同。底部提醒：“不是小明的词义变了，而是小明这个位置的表示被上下文化”。博客手帐感、奶油白背景、鼠尾草绿、雾蓝、橙色强调。</p>
        </details>
      </figure>
    </section>

    <section id="sentence-vector" class="content-section">
      <p class="section-label">06 · 最初那个问题终于可以回答了</p>
      <h2>「小明打了小红」最终是怎么得到一个 Vector 的？</h2>

      <p>理解到这里，我又提出了一个很自然的猜测：</p>
      <div class="question-card">
        <span>当时的猜测</span>
        <strong>是不是“小明 + 打了”先算出一个中间值，再用这个值 + 小红，最后得到整句话的 Vector？</strong>
      </div>

      <p>答案是否定的。这个猜测的问题在于，它把 Transformer 想成了从左到右的“累加器”。更接近真实过程的是：</p>
      <div class="wrong-flow">
        <span>小明</span><b>+</b><span>打了</span><i>→</i><span>中间值 X</span><b>+</b><span>小红</span><i>→</i><strong>最终 Vector</strong><em>✕</em>
      </div>

      <div class="correct-flow">
        <div><span>小明</span><small>初始 A</small></div>
        <div><span>打了</span><small>初始 B</small></div>
        <div><span>小红</span><small>初始 C</small></div>
        <b>↓</b>
        <strong>多层 Transformer：三个位置反复交换上下文信息</strong>
        <b>↓</b>
        <div><span>h₁</span><small>上下文化后的小明位置</small></div>
        <div><span>h₂</span><small>上下文化后的打了位置</small></div>
        <div><span>h₃</span><small>上下文化后的小红位置</small></div>
        <b>↓</b>
        <strong>Pooling / 模型指定的汇总方式</strong>
        <b>↓</b>
        <strong>Sentence Vector</strong>
      </div>

      <p>这里的 <strong>Pooling</strong> 才是“很多 Token 表示最终怎么变成一个句子 / Chunk 表示”的关键一步。</p>
      <p>最容易理解的一种方式是 Mean Pooling：把多个上下文化 Token Vector 按维度求平均。但实际 Embedding 模型还可能使用 CLS、Last Token 或专门训练的 Pooling 方式。</p>

      <div class="pooling-flow">
        <div><span>h₁</span><small>小明位置</small></div>
        <div><span>h₂</span><small>打了位置</small></div>
        <div><span>h₃</span><small>小红位置</small></div>
        <b>→</b><strong>Pooling</strong><b>→</b><div class="sentence"><span>[0.31, -0.72, 0.48, ...]</span><small>Sentence Vector</small></div>
      </div>

      <p>于是之前 RAG 那个疑惑也终于能回答：</p>
      <blockquote><strong>“整段文字 → 一个 Vector”只是把中间过程省略了。</strong><br />内部仍然是 Text → Token → 初始 Token Vector → 上下文化 Token 表示 → Pooling → Chunk Vector。</blockquote>

      <figure class="image-placeholder compact">
        <div class="placeholder-art">IMAGE · 03</div>
        <figcaption><strong>待生成配图：RAG 的“整段向量化”到底省略了什么</strong><span>从 HashMap 示例重新连接回 RAG。</span></figcaption>
        <details>
          <summary>查看配图提示词</summary>
          <p>横版中文 RAG 教学信息图。左侧原文“HashMap 底层使用数组和链表 / 红黑树”；依次经过 Tokenizer → 多个 Token → Token ID → 查 Embedding Table 得到初始 Token Vector → Transformer 形成多个上下文化 Hidden State → Pooling → 一个 Chunk Vector → Vector DB。顶部用红色小字标出常见简写“Text → Embedding → Vector”，下面展开真实中间过程。底部强调：“整段向量化 ≠ 不分 Token；只是中间过程经常被教程省略。”风格匹配个人博客，清晰、克制、不要复杂公式。</p>
        </details>
      </figure>
    </section>

    <section id="migration" class="content-section">
      <p class="section-label">07 · 从原理落回工程</p>
      <h2>既然 Vector 是模型学出来的坐标，那更换 Embedding 模型会发生什么？</h2>

      <p>理解了“Embedding Model 最终是在建立一套向量空间”以后，我顺着 RAG 又想到另一个问题：<strong>如果知识库已经全部向量化了，后面换一个 Embedding 模型，旧向量还能不能继续用？</strong></p>

      <p>例如知识库里有：</p>
      <pre><code>Chunk A：HashMap 底层使用数组、链表和红黑树……
Chunk B：ConcurrentHashMap 支持并发访问……
Chunk C：ArrayList 底层使用动态数组……</code></pre>
      <p>使用 Embedding Model A 后：</p>
      <pre><code>Chunk A → Vector A₁
Chunk B → Vector B₁
Chunk C → Vector C₁</code></pre>
      <p>用户查询也必须进入同一个空间：</p>
      <div class="rag-flow"><span>用户问题</span><i>→</i><strong>Embedding Model A</strong><i>→</i><span>Query Vector A</span><b>↔ Similarity ↔</b><span>知识库 Vector A₁/B₁/C₁</span></div>

      <p>如果查询突然改用 Embedding Model B，那么即使它也输出 768 维：</p>
      <pre><code>Model A 的 768 维空间
≠
Model B 的 768 维空间</code></pre>
      <p>原因不是“维度不一样”，而是<strong>每一维以及整个空间的相对结构都是由不同训练过程学出来的</strong>。它们更像两套不同的坐标系。</p>

      <div class="question-card caution">
        <span>迁移后果</span>
        <strong>更换 Embedding 模型，通常不能只改查询侧配置。知识库中的历史 Chunk 也需要用新模型重新向量化。</strong>
      </div>

      <p>这会直接带来几项工程后果：</p>
      <div class="impact-grid">
        <div><b>重新 Embedding</b><p>原始文档 / Chunk 需要重新跑一遍新模型，生成一套全新的 Vector。</p></div>
        <div><b>重新建索引</b><p>HNSW、IVF 等索引通常需要基于新 Vector 重新构建。</p></div>
        <div><b>旧分数失去可比性</b><p>原来的相似度阈值、Top K 分布可能发生变化，不能默认沿用。</p></div>
        <div><b>重新评测</b><p>需要重新看 Recall、命中质量和业务问答效果，确认迁移到底有没有变好。</p></div>
      </div>

      <p>所以我现在会把“选 Embedding 模型”理解成一个比最初想象中更重的架构决策：它不只是换一个 API，而是在决定<strong>整个知识库以后使用哪套语义坐标系</strong>。</p>

      <figure class="image-placeholder compact">
        <div class="placeholder-art">IMAGE · 04</div>
        <figcaption><strong>待生成配图：Embedding 模型迁移的真实后果</strong><span>把“不能混用”落到工程成本。</span></figcaption>
        <details>
          <summary>查看配图提示词</summary>
          <p>16:9 中文工程信息图，标题“更换 Embedding 模型，不只是换一个接口”。左侧旧系统：原始 Chunk → Embedding Model A → Vector A → Vector DB / HNSW Index；查询也用 Model A。中间用醒目箭头标出“切换到 Model B”。右侧展示迁移工作：原始 Chunk 全量重新 Embedding → 新 Vector B → 重新建立索引 → 重新评估相似度阈值 / Recall / Top K。用红色叉号标注“Model B Query Vector 直接搜索 Model A Vector：不可直接混用”。底部总结：“维度相同 ≠ 向量空间相同”。风格温暖、克制、技术博客手帐感。</p>
        </details>
      </figure>
    </section>

    <section id="review" class="review-card">
      <p class="section-label">这次学习后，我现在怎么理解</p>
      <h2>不是背定义，而是把因果链串起来</h2>
      <ol>
        <li><strong>起点：</strong>我想知道 Parameter 怎么和 Token 计算，于是先发现 Token 必须先变成数字表示。</li>
        <li><strong>第一个澄清：</strong>Token 不是 Parameter；Token 是输入，Token ID 是编号，Embedding Table 里的数字才属于模型参数。</li>
        <li><strong>第二个澄清：</strong>Embedding Table 的数字是训练阶段学出来的；推理阶段根据 Token ID 查表，不是在现场重新训练。</li>
        <li><strong>第一次迁移：</strong>想到 RAG 后，我发现“整段文字一个 Vector”不能用“一个 Token 一个 Vector”的理解直接解释。</li>
        <li><strong>关键例子：</strong>用「小明打了小红 / 小红打了小明」发现，仅有 Token 集合不够，模型还必须编码顺序和上下文关系。</li>
        <li><strong>重要纠正：</strong>不是“小明的词义变了”，而是“小明这个位置的 Hidden State 被上下文化”。</li>
        <li><strong>最终链路：</strong>多个 Token 先各自表示，再经过多层 Transformer 互相吸收上下文，最后通过 Pooling 等方式汇总成 Sentence / Chunk Vector。</li>
        <li><strong>工程后果：</strong>不同 Embedding 模型通常对应不同向量空间，换模型往往意味着重新向量化、重建索引和重新评测。</li>
      </ol>
      <div class="one-line">我现在最想记住的一句话：<strong>先拆成 Token → 各自得到初始表示 → 通过上下文建模互相影响 → 最后汇总成整个句子 / Chunk 的 Vector。</strong></div>
    </section>

    <section id="next" class="interview-card">
      <p class="section-label">还没有解决的问题</p>
      <h2>这篇到这里，不假装所有细节都已经懂了</h2>
      <p>这次只是把“文字怎样一路走到 Sentence / Chunk Vector”这条主链搭起来。过程中其实又留下了几个新的问题：</p>
      <details open><summary>Q1：Attention 到底怎么计算“该关注谁”？</summary><p>下一步需要真正理解 Q、K、V，以及为什么点积可以表示相关程度。</p></details>
      <details><summary>Q2：Pooling 把几百个 Token 压成一个 Vector，不会丢信息吗？</summary><p>会，这会自然引出 Chunk Size、Overlap、Embedding 模型训练目标，以及后面的 Reranker。</p></details>
      <details><summary>Q3：两个 Chunk Vector 到底怎么判断“像不像”？</summary><p>这就是下一篇要进入的 Cosine Similarity / Dot Product / Euclidean Distance。</p></details>
      <details><summary>Q4：为什么 Embedding 模型能把“语义相似”训练成“空间接近”？</summary><p>这涉及 Embedding 模型自己的训练目标、正负样本和对比学习，暂时先留作后续继续追。</p></details>
    </section>

    <nav class="next-article">
      <span>NEXT · 03</span>
      <strong>两个 Vector 到底怎么判断“像不像”？</strong>
      <small>下一篇从余弦相似度开始，把 RAG 检索里真正发生的 Vector → Similarity → Top K 算清楚。</small>
    </nav>
  </article>
</BaseLayout>

<style>
  .article-page { max-width: 1040px; padding-block: 42px 80px; }
  .back-link { display: inline-block; margin-bottom: 24px; color: var(--ink-faint); font-size: 12px; }
  .article-hero { display: grid; grid-template-columns: 1.35fr .65fr; gap: 38px; align-items: center; padding: 34px; border: 1px solid var(--line); border-radius: 22px; background: rgba(255,253,248,.72); box-shadow: var(--shadow); }
  .series, .section-label { margin: 0; color: var(--sage); font-size: 11px; letter-spacing: .14em; font-weight: 700; }
  .series-link { display: inline-block; text-decoration: none; }
  .article-hero h1 { margin: 10px 0 14px; font-family: var(--hand-font); font-size: clamp(34px, 4vw, 52px); line-height: 1.25; }
  .subtitle { margin: 0; color: var(--ink-soft); font-family: var(--body-font); line-height: 1.9; }
  .meta { display: flex; gap: 18px; margin-top: 20px; color: var(--ink-faint); font-size: 11px; }
  .hero-flow { display: grid; gap: 7px; justify-items: center; padding: 24px; border: 1px solid #d7ddcf; border-radius: 18px; background: linear-gradient(160deg, #f3f6ee, #fbf8f1); }
  .hero-flow span { min-width: 150px; padding: 9px 12px; border: 1px solid #c7d3bf; border-radius: 10px; background: #fffdf8; color: #596052; font-family: var(--body-font); font-size: 12px; font-weight: 700; text-align: center; }
  .hero-flow i { color: var(--sage); font-style: normal; }

  .goal-card, .review-card, .interview-card { margin-top: 26px; padding: 26px 30px; border: 1px solid #cbd6c2; border-radius: 18px; background: linear-gradient(145deg, #eef3e9, #f8f7f1); }
  .goal-card > p, .interview-card > p { color: #565348; font-family: var(--body-font); line-height: 1.9; }
  .review-card ol { margin: 14px 0 0; padding-left: 22px; color: #565348; font-family: var(--body-font); line-height: 1.9; }
  .content-section { padding: 48px 8px 0; scroll-margin-top: 88px; }
  .review-card, .interview-card { scroll-margin-top: 88px; }
  .content-section h2, .review-card h2, .interview-card h2 { margin: 8px 0 16px; font-family: var(--hand-font); font-size: 30px; }
  .content-section h3 { margin: 26px 0 10px; color: #4f4c44; font-family: var(--body-font); font-size: 17px; }
  .content-section > p { color: #58554b; font-family: var(--body-font); font-size: 15px; line-height: 1.95; }
  pre { overflow-x: auto; margin: 18px 0; padding: 20px 22px; border: 1px solid var(--line); border-radius: 14px; background: #f8f6ef; color: #514e47; font-size: 13px; line-height: 1.75; }
  code { font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace; }
  blockquote { margin: 20px 0; padding: 16px 20px; border-left: 3px solid var(--sage); background: rgba(239,243,234,.72); color: #55594f; font-family: var(--body-font); line-height: 1.8; }
  .note { padding: 14px 16px; border: 1px dashed #c8d3c0; border-radius: 12px; background: #f8faf5; }

  .question-chain, .mini-flow { display: flex; flex-wrap: wrap; gap: 8px; align-items: center; margin: 18px 0 4px; }
  .question-chain span, .mini-flow span { padding: 9px 11px; border: 1px solid #cad4c3; border-radius: 9px; background: rgba(255,253,248,.8); color: #5c6256; font-size: 12px; font-weight: 700; }
  .question-chain i, .mini-flow i { color: var(--sage); font-style: normal; }

  .question-card { display: grid; gap: 6px; margin: 18px 0; padding: 18px 20px; border: 1px solid #d6cbb7; border-radius: 14px; background: linear-gradient(145deg, #fbf6e9, #fffdf7); }
  .question-card span { color: #a78251; font-size: 10px; font-weight: 800; letter-spacing: .12em; }
  .question-card strong { color: #5e5548; font-family: var(--body-font); line-height: 1.7; }
  .question-card.caution { border-color: #dfc5ae; background: #fff7ef; }

  .concept-grid { display: grid; gap: 12px; margin: 18px 0; }
  .concept-grid.three { grid-template-columns: repeat(3, 1fr); }
  .concept-grid.two { grid-template-columns: repeat(2, 1fr); }
  .concept-grid > div { padding: 18px; border: 1px solid var(--line); border-radius: 14px; background: rgba(255,253,248,.8); }
  .concept-grid .focus { border-color: #bdcbb3; background: #f0f5ec; }
  .concept-grid b, .concept-grid code { display: block; margin-bottom: 8px; }
  .concept-grid p { margin: 0; color: var(--ink-soft); font-size: 12px; line-height: 1.7; }

  .phase-compare { display: grid; grid-template-columns: repeat(2, 1fr); gap: 14px; margin: 18px 0; }
  .phase-compare > div { padding: 18px; border: 1px solid var(--line); border-radius: 14px; background: rgba(255,253,248,.76); }
  .phase-compare small { color: var(--sage); font-size: 10px; font-weight: 800; letter-spacing: .12em; }
  .phase-compare h3 { margin: 7px 0 12px; }
  .phase-compare pre { margin-bottom: 0; }

  .contrast-sentences { display: grid; grid-template-columns: repeat(2, 1fr); gap: 14px; margin: 18px 0; }
  .contrast-sentences > div { display: grid; gap: 7px; padding: 18px; border: 1px solid #ced8c6; border-radius: 14px; background: #f7f9f3; text-align: center; }
  .contrast-sentences small { color: var(--sage); font-weight: 800; }
  .contrast-sentences strong { color: #55564f; font-size: 18px; }

  .token-row, .position-flow { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; margin: 18px 0; }
  .token-row span, .position-flow span { display: flex; flex-direction: column; align-items: center; gap: 6px; padding: 14px; border: 1px solid #cbd6c3; border-radius: 12px; background: #f5f8f1; color: #50574b; font-weight: 700; }
  .token-row small { color: var(--ink-faint); font-weight: 500; }
  .attention-demo { display: grid; grid-template-columns: 1fr 1.4fr 1fr; gap: 12px; align-items: stretch; margin: 18px 0; }
  .attention-demo > div { padding: 18px; border: 1px solid var(--line); border-radius: 14px; }
  .attention-demo .source { display: grid; gap: 8px; background: #faf8f2; }
  .attention-demo .source span { padding: 7px 10px; border-radius: 8px; background: #fffdf8; font-size: 12px; }
  .attention-demo .meeting { background: #eef4ea; }
  .attention-demo .meeting strong { color: var(--sage-dark); }
  .attention-demo .meeting p { color: #5b6155; font-size: 12px; line-height: 1.7; }
  .attention-demo .result { border-color: #c1d0b8; background: #f2f7ee; }

  .wrong-flow { display: flex; flex-wrap: wrap; gap: 10px; align-items: center; margin: 18px 0; padding: 20px; border: 1px solid #e0c5bd; border-radius: 14px; background: #fbf3ef; color: #704f47; font-family: var(--body-font); }
  .wrong-flow span, .wrong-flow strong { padding: 8px 12px; border: 1px solid #dfbdb3; border-radius: 9px; background: #fffaf7; }
  .wrong-flow b, .wrong-flow i, .wrong-flow em { color: #b86d5c; font-style: normal; }
  .correct-flow { display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px; align-items: stretch; margin: 20px 0; }
  .correct-flow > div { display: flex; flex-direction: column; gap: 5px; padding: 14px; border: 1px solid #c9d5c1; border-radius: 12px; background: #f5f8f1; text-align: center; }
  .correct-flow > b, .correct-flow > strong { grid-column: 1 / -1; text-align: center; color: #64705e; }
  .correct-flow small { color: var(--ink-faint); }

  .pooling-flow { display: flex; flex-wrap: wrap; gap: 10px; align-items: center; margin: 20px 0; padding: 18px; border: 1px solid var(--line); border-radius: 14px; background: #faf8f2; }
  .pooling-flow > div { display: flex; flex-direction: column; gap: 5px; padding: 10px 12px; border: 1px solid #ced8c6; border-radius: 10px; background: #fffdf8; text-align: center; }
  .pooling-flow > strong { padding: 10px 14px; border-radius: 10px; background: #eaf1e5; color: #586352; }
  .pooling-flow .sentence { border-color: #b9c9ae; background: #f0f6eb; }
  .pooling-flow small { color: var(--ink-faint); }

  .rag-flow { display: flex; flex-wrap: wrap; gap: 8px; align-items: center; justify-content: center; margin: 20px 0; padding: 20px; border: 1px solid #cad6c2; border-radius: 14px; background: #f5f8f1; }
  .rag-flow span, .rag-flow strong { padding: 8px 10px; border-radius: 8px; background: #fffdf8; }
  .rag-flow i, .rag-flow b { color: var(--sage); font-style: normal; font-size: 11px; }

  .impact-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 12px; margin: 18px 0; }
  .impact-grid > div { padding: 18px; border: 1px solid #d1d8ca; border-radius: 14px; background: #faf9f4; }
  .impact-grid b { display: block; margin-bottom: 7px; color: #596152; }
  .impact-grid p { margin: 0; color: #6a675f; font-size: 12px; line-height: 1.7; }

  .image-placeholder { margin: 26px 0 10px; padding: 16px; border: 1px dashed #bfcbb7; border-radius: 16px; background: linear-gradient(145deg, #f4f7ef, #fbf9f3); }
  .image-placeholder.compact .placeholder-art { min-height: 170px; }
  .placeholder-art { display: grid; min-height: 230px; place-items: center; border: 1px solid #d9dfd3; border-radius: 12px; background: repeating-linear-gradient(135deg, #f8f7f1, #f8f7f1 14px, #f3f4ee 14px, #f3f4ee 28px); color: #98a090; font-size: 12px; font-weight: 800; letter-spacing: .16em; }
  .image-placeholder figcaption { display: flex; flex-direction: column; gap: 4px; padding: 12px 4px 4px; color: #5f6259; font-size: 12px; }
  .image-placeholder figcaption span { color: var(--ink-faint); }
  .image-placeholder details { margin-top: 8px; padding: 10px 12px; border-radius: 10px; background: rgba(255,253,248,.7); }
  .image-placeholder summary { cursor: pointer; color: var(--sage-dark); font-size: 11px; font-weight: 700; }
  .image-placeholder details p { color: #69655c; font-size: 12px; line-height: 1.75; }

  .one-line { margin-top: 18px; padding: 14px 16px; border-radius: 12px; background: rgba(255,253,248,.78); color: #575a51; }
  .interview-card details { padding: 12px 0; border-bottom: 1px solid rgba(120,128,110,.18); }
  .interview-card summary { cursor: pointer; color: #50554d; font-family: var(--body-font); font-weight: 700; }
  .interview-card details p { color: #67635b; font-family: var(--body-font); font-size: 13px; line-height: 1.75; }
  .next-article { display: grid; gap: 7px; margin-top: 30px; padding: 24px 26px; border: 1px solid var(--line); border-radius: 16px; background: rgba(255,253,248,.72); }
  .next-article span { color: var(--sage); font-size: 10px; font-weight: 800; letter-spacing: .14em; }
  .next-article strong { color: #4f4b43; font-family: var(--hand-font); font-size: 23px; }
  .next-article small { color: var(--ink-faint); line-height: 1.6; }

  @media (max-width: 760px) {
    .article-page { width: min(100% - 28px, 1040px); padding-top: 24px; }
    .article-hero { grid-template-columns: 1fr; padding: 24px 22px; }
    .concept-grid.three, .concept-grid.two, .phase-compare, .attention-demo, .contrast-sentences, .impact-grid { grid-template-columns: 1fr; }
    .correct-flow, .token-row, .position-flow { grid-template-columns: 1fr; }
    .meta { flex-wrap: wrap; }
    .content-section { padding-top: 38px; }
  }
</style>