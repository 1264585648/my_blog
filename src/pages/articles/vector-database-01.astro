---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { withBase } from '../../utils/paths';
---

<BaseLayout
  title="01｜向量数据库究竟在解决什么问题？ · xuan.z 的个人笔记"
  description="Build Your Own Vector Database 系列第一篇：从 SQL、关键词检索一路推导到语义检索，理解向量数据库为什么出现。"
>
  <article class="article-page shell">
    <a class="back-link" href={withBase('/articles/')}>← 返回文章</a>

    <header class="article-hero">
      <div class="hero-copy">
        <p class="series">BUILD YOUR OWN VECTOR DATABASE · 01</p>
        <h1>向量数据库究竟在解决什么问题？</h1>
        <p class="subtitle">先不学 HNSW，也先不记任何数据库产品。我们从一个最朴素的问题出发：怎样从大量内容里找到“意思最接近”的那几条？</p>
        <div class="meta"><span>2026-08-11</span><span>约 15 分钟</span><span>原理入门</span></div>
      </div>
      <img src={withBase('/assets/article-vector-db.svg')} alt="向量数据库语义检索示意图" />
    </header>

    <section class="goal-card">
      <p class="section-label">本章目标</p>
      <ul>
        <li>区分结构化查询、关键词检索和语义检索。</li>
        <li>理解 Text → Embedding → Vector → Similarity → Top K 这条最小链路。</li>
        <li>能用自己的话解释：为什么已经有 MySQL / Elasticsearch，还需要 Vector DB。</li>
      </ul>
    </section>

    <section class="content-section">
      <p class="section-label">01 · 从一个问题开始</p>
      <h2>如果没有向量数据库，你会怎么搜？</h2>
      <p>假设知识库里有 100 万篇技术文章，用户输入：</p>
      <div class="query-box">“Java 后端怎么实现分布式任务调度？”</div>
      <p>我们真正想找到的，不一定是包含完全相同词语的文章，而是<strong>语义上最相关</strong>的内容。</p>

      <div class="doc-grid">
        <div class="doc-card"><span>A</span><p>XXL-JOB 的调度中心通过 Executor 执行任务。</p></div>
        <div class="doc-card"><span>B</span><p>Spring Boot 中如何配置定时任务。</p></div>
        <div class="doc-card muted"><span>C</span><p>北京今天可能有阵雨。</p></div>
      </div>

      <div class="think-card">
        <strong>先想 30 秒</strong>
        <p>如果只允许你用 MySQL 的等值查询或 LIKE，你会怎么找到 A 和 B？它会遇到什么问题？</p>
      </div>
    </section>

    <section class="content-section">
      <p class="section-label">02 · 三种“找东西”的方式</p>
      <h2>数据库并不是都在解决同一种搜索问题</h2>
      <div class="compare-list">
        <div><b>结构化查询</b><code>WHERE user_id = 1001</code><p>我们知道字段和值，目标是精确定位。</p></div>
        <div><b>关键词检索</b><code>“分布式” AND “任务”</code><p>我们知道可能出现的词，目标是找到包含这些词的文档。</p></div>
        <div class="focus"><b>语义检索</b><code>“和这句话意思最接近的内容”</code><p>我们不知道答案会使用什么词，只知道它应该表达相似的含义。</p></div>
      </div>
      <p>向量数据库重点服务的是第三类问题：<strong>相似性搜索（Similarity Search）</strong>。</p>
    </section>

    <section class="content-section">
      <p class="section-label">03 · 把“意思”变成可以计算的东西</p>
      <h2>Embedding 是语义检索的桥梁</h2>
      <p>计算机没法直接计算“两句话的意思有多像”。Embedding 模型做的关键工作，是把文本映射成一个高维向量。</p>

      <div class="pipeline" aria-label="语义检索最小链路">
        <span>文本</span><i>→</i><span>Embedding</span><i>→</i><span>Vector</span><i>→</i><span>Similarity</span><i>→</i><span>Top K</span>
      </div>

      <pre><code>文章 A → [ 0.21, -0.37, 0.82, ... ]
文章 B → [ 0.18, -0.31, 0.79, ... ]
文章 C → [-0.71,  0.12, 0.03, ... ]

用户问题 → [ 0.20, -0.35, 0.81, ... ]</code></pre>

      <p>接下来只需要回答一个数学问题：<strong>哪个向量离 Query Vector 最近？</strong></p>
      <p class="note">这一章先接受“Embedding 能把相近语义映射到相近位置”这个事实。下一章我们再拆向量、维度、Embedding 和距离度量。</p>
    </section>

    <section class="content-section">
      <p class="section-label">04 · 最小 Vector DB 长什么样</p>
      <h2>先忘掉 Milvus 和 Qdrant</h2>
      <p>一个最小可工作的向量检索系统，其实只需要保存三类信息：</p>
      <pre><code>{
  "id": "doc_001_chunk_03",
  "vector": [0.21, -0.37, 0.82, ...],
  "text": "XXL-JOB 的调度中心通过 Executor 执行任务。",
  "metadata": {
    "project": "xxl-job",
    "language": "java"
  }
}</code></pre>
      <p>最简单的搜索方式甚至不需要复杂索引：</p>
      <div class="steps">
        <span>① 把 Query 转成向量</span>
        <span>② 和所有向量计算相似度</span>
        <span>③ 按分数排序</span>
        <span>④ 返回 Top K</span>
      </div>
      <p>这就是我们后面会亲手实现的第一版：<strong>Flat / Brute-force Search</strong>。</p>
    </section>

    <section class="content-section">
      <p class="section-label">05 · 真正的问题才刚开始</p>
      <h2>为什么不能永远暴力扫描？</h2>
      <p>假设数据库从几百条数据增长到 100 万、1000 万甚至更多，每次查询都和全部向量逐个比较，成本会快速上升。</p>
      <div class="think-card strong">
        <strong>本章最重要的推导题</strong>
        <p>假设有 100 万个 1536 维向量。一次查询如果扫描全部数据，需要做多少量级的数字比较？你觉得最先遇到的是 CPU、内存还是延迟问题？</p>
        <small>暂时不要查 HNSW。先尝试从“怎么少看一些数据”这个方向自己设计一种方案。</small>
      </div>
      <p>下一阶段的 ANN、IVF、HNSW，本质上都在回答同一个问题：</p>
      <blockquote>能不能不检查全部向量，也大概率找到真正最相似的那些结果？</blockquote>
    </section>

    <section class="review-card">
      <p class="section-label">本章复盘</p>
      <h2>现在应该能说清楚三句话</h2>
      <ol>
        <li>传统结构化查询擅长“精确匹配”，全文检索擅长“词语匹配”，Vector Search 擅长“相似性匹配”。</li>
        <li>Embedding 把内容变成向量，相似度计算把“语义接近”转成一个可以排序的问题。</li>
        <li>Vector DB 的核心挑战不是“能不能存 float[]”，而是大量向量下怎样快速、稳定地做 Top K 相似性搜索。</li>
      </ol>
    </section>

    <section class="interview-card">
      <div>
        <p class="section-label">面试自检</p>
        <h2>先别背答案，试着自己讲</h2>
      </div>
      <div class="questions">
        <details><summary>Q1：MySQL 已经能存数组，为什么还需要向量数据库？</summary><p>回答时重点区分“存储能力”和“大规模相似性检索能力”。</p></details>
        <details><summary>Q2：Elasticsearch 和 Vector DB 的搜索目标有什么差异？</summary><p>先从关键词匹配与语义相似性两个目标切入；后面我们会继续讨论 Hybrid Search。</p></details>
        <details><summary>Q3：向量数据库最核心的性能矛盾是什么？</summary><p>先记住三个词：Recall、Latency、Memory。后续每一种 ANN 算法都围绕它们做权衡。</p></details>
      </div>
    </section>

    <nav class="next-article">
      <span>NEXT · 02</span>
      <strong>Vector、Embedding 与相似度：机器到底怎样表示“相近”？</strong>
      <small>下一篇我们从 float[] 开始，真正进入向量检索的数学基础。</small>
    </nav>
  </article>
</BaseLayout>

<style>
  .article-page { max-width: 1040px; padding-block: 42px 80px; }
  .back-link { display: inline-block; margin-bottom: 24px; color: var(--ink-faint); font-family: var(--body-font); font-size: 12px; }
  .back-link:hover { color: var(--sage-dark); }
  .article-hero { display: grid; grid-template-columns: 1.35fr .65fr; gap: 38px; align-items: center; padding: 34px; border: 1px solid var(--line); border-radius: 22px; background: rgba(255,253,248,.72); box-shadow: var(--shadow); }
  .article-hero img { width: 100%; border-radius: 16px; border: 1px solid var(--line-soft); }
  .series, .section-label { margin: 0; color: var(--sage); font-size: 11px; letter-spacing: .14em; font-weight: 700; }
  .article-hero h1 { margin: 10px 0 14px; font-family: var(--hand-font); font-size: clamp(34px, 4vw, 52px); line-height: 1.25; }
  .subtitle { margin: 0; max-width: 680px; color: var(--ink-soft); font-family: var(--body-font); line-height: 1.9; }
  .meta { display: flex; gap: 18px; margin-top: 20px; color: var(--ink-faint); font-size: 11px; }
  .goal-card, .review-card, .interview-card { margin-top: 26px; padding: 26px 30px; border: 1px solid #cbd6c2; border-radius: 18px; background: linear-gradient(145deg, #eef3e9, #f8f7f1); }
  .goal-card ul, .review-card ol { margin: 14px 0 0; padding-left: 22px; color: #565348; font-family: var(--body-font); line-height: 1.9; }
  .content-section { padding: 48px 8px 0; }
  .content-section h2, .review-card h2, .interview-card h2 { margin: 8px 0 16px; font-family: var(--hand-font); font-size: 30px; }
  .content-section > p { color: #58554b; font-family: var(--body-font); font-size: 15px; line-height: 1.95; }
  .query-box { margin: 18px 0; padding: 18px 22px; border-left: 3px solid var(--sage); background: rgba(255,253,248,.7); font-family: var(--body-font); font-size: 19px; }
  .doc-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 14px; margin: 18px 0; }
  .doc-card { min-height: 120px; padding: 16px; border: 1px solid #cad7c1; border-radius: 14px; background: #f4f7ef; }
  .doc-card.muted { border-color: var(--line); background: #f4efe5; }
  .doc-card span { display: grid; width: 28px; height: 28px; place-items: center; border-radius: 50%; background: rgba(255,255,255,.7); color: var(--sage-dark); font-weight: 700; font-size: 12px; }
  .doc-card p { margin: 12px 0 0; color: #555248; font-family: var(--body-font); line-height: 1.7; font-size: 13px; }
  .think-card { margin: 20px 0; padding: 20px 22px; border: 1px dashed #b9ad98; border-radius: 14px; background: #faf6ec; }
  .think-card.strong { border-style: solid; border-color: #d8bd8d; background: #faf1df; }
  .think-card strong { font-family: var(--body-font); }
  .think-card p { margin: 8px 0 0; color: #5d584d; font-family: var(--body-font); line-height: 1.8; }
  .think-card small { display: block; margin-top: 8px; color: var(--ink-faint); line-height: 1.6; }
  .compare-list { display: grid; gap: 12px; margin: 20px 0; }
  .compare-list > div { display: grid; grid-template-columns: 130px 1.2fr 1.5fr; gap: 18px; align-items: center; padding: 16px 18px; border: 1px solid var(--line); border-radius: 12px; background: rgba(255,253,248,.68); }
  .compare-list .focus { border-color: #bdccb3; background: #f1f5ec; }
  .compare-list code { color: #655e52; font-size: 12px; }
  .compare-list p { margin: 0; color: var(--ink-soft); font-family: var(--body-font); font-size: 12px; }
  .pipeline { display: flex; flex-wrap: wrap; align-items: center; justify-content: center; gap: 9px; margin: 24px 0; padding: 22px; border: 1px solid var(--line); border-radius: 16px; background: rgba(255,253,248,.66); }
  .pipeline span { padding: 8px 12px; border-radius: 9px; background: #eaf0e5; color: #546149; font-size: 12px; font-weight: 700; }
  .pipeline i { color: #9c9486; font-style: normal; }
  pre { overflow-x: auto; margin: 20px 0; padding: 20px 22px; border: 1px solid #d9d0c1; border-radius: 14px; background: #2f302c; color: #ebe8df; font-size: 12px; line-height: 1.8; }
  .note { padding: 14px 16px; border-radius: 10px; background: #edf2e8; }
  .steps { display: grid; grid-template-columns: repeat(4, 1fr); gap: 10px; margin: 20px 0; }
  .steps span { padding: 14px; border: 1px solid var(--line); border-radius: 12px; background: rgba(255,253,248,.7); color: #5b574e; font-family: var(--body-font); font-size: 12px; text-align: center; }
  blockquote { margin: 24px 0 0; padding: 20px 24px; border-left: 3px solid var(--sage); color: #4e5748; background: #f1f5ec; font-family: var(--body-font); font-size: 17px; line-height: 1.8; }
  .review-card { margin-top: 48px; }
  .interview-card { margin-top: 18px; background: linear-gradient(145deg, #f6efe2, #faf8f1); border-color: #e2d4bb; }
  .questions { display: grid; gap: 10px; margin-top: 18px; }
  details { border-top: 1px solid rgba(150,137,111,.25); padding: 14px 0 2px; }
  summary { cursor: pointer; font-family: var(--body-font); font-weight: 700; color: #514d45; }
  details p { margin: 10px 0 0; color: var(--ink-soft); font-family: var(--body-font); line-height: 1.7; font-size: 13px; }
  .next-article { display: flex; flex-direction: column; gap: 7px; margin-top: 28px; padding: 24px 28px; border: 1px solid var(--line); border-radius: 16px; background: rgba(255,253,248,.7); }
  .next-article span { color: var(--sage); font-size: 10px; letter-spacing: .16em; }
  .next-article strong { font-family: var(--body-font); font-size: 16px; }
  .next-article small { color: var(--ink-faint); font-family: var(--body-font); }
  @media (max-width: 820px) {
    .article-page { width: min(100% - 28px, 1040px); padding-top: 24px; }
    .article-hero { grid-template-columns: 1fr; padding: 24px; }
    .article-hero img { max-width: 420px; }
    .doc-grid, .steps { grid-template-columns: 1fr 1fr; }
    .compare-list > div { grid-template-columns: 1fr; gap: 7px; }
  }
  @media (max-width: 520px) {
    .doc-grid, .steps { grid-template-columns: 1fr; }
    .meta { flex-wrap: wrap; }
    .article-hero h1 { font-size: 34px; }
    .content-section { padding-top: 38px; }
  }
</style>
