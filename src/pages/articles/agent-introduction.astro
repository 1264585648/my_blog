---
import LearningArticleLayout from '../../layouts/LearningArticleLayout.astro';

const tocItems = [
  { id: 'task-first', label: '先看任务，不背定义', hint: '从“回答问题”与“完成任务”的差别开始' },
  { id: 'boundary', label: 'Agent 的边界到底在哪', hint: 'LLM、Chatbot、Workflow 与 Agent' },
  { id: 'definition', label: '给 Agent 一个真正可用的定义', hint: '目标、决策、行动、反馈' },
  { id: 'loop', label: 'Agent 是怎么跑起来的', hint: 'Decision → Action → Observation' },
  { id: 'tools', label: 'Tool 为什么这么重要', hint: '让模型从“说”走向“做”' },
  { id: 'when', label: '什么时候该用 Agent', hint: '不是所有 LLM 应用都值得 Agent 化' },
  { id: 'minimal', label: '把 Agent 压缩成最小模型', hint: '用一段伪代码串起整篇文章' }
];

const readings = [
  {
    tag: '主教材',
    title: 'Hugging Face Agents Course · Introduction to Agents',
    href: 'https://huggingface.co/learn/agents-course/unit1/introduction',
    note: '适合继续学习 Tool、Action、Observation 与 Agent Loop。'
  },
  {
    tag: '工程视角',
    title: 'Anthropic · Building Effective Agents',
    href: 'https://www.anthropic.com/engineering/building-effective-agents',
    note: '重点理解 Workflow 与 Agent 的边界，以及为什么应该优先从简单方案开始。'
  },
  {
    tag: '实践指南',
    title: 'OpenAI · A Practical Guide to Building Agents',
    href: 'https://openai.com/business/guides-and-resources/a-practical-guide-to-building-ai-agents/',
    note: '重点看 Model、Tools、Instructions 与适用场景。'
  }
];

const minimalAgent = `context = [user_goal]

while True:
    decision = model.decide(
        instructions=instructions,
        context=context,
        tools=tools,
    )

    if decision.type == "finish":
        return decision.answer

    observation = tools.run(decision.tool_call)
    context.append(observation)`;
---

<LearningArticleLayout
  title="什么是 AI Agent？"
  description="从一个真实任务出发，理解 Agent 与普通 LLM、Chatbot、Workflow 的差别，并用 Agent Loop 建立最小心智模型。"
  seriesLabel="AGENT LEARNING · 01"
  seriesHref="/learning/"
  subtitle="不从术语开始。先看一件普通 LLM 做不到、但 Agent 可以一步步完成的事。"
  date="2026-08-12"
  readingTime="约 12 分钟"
  level="基础认知"
  heroImage="/assets/learning-agent-introduction.svg"
  heroAlt="Agent 围绕目标进行决策、调用工具并观察结果的循环示意图"
  tocItems={tocItems}
  readings={readings}
  next={{
    label: 'NEXT · 02',
    title: 'Agent Framework：框架到底帮我们做了什么？',
    href: '/learning/',
    note: '下一篇再把 Model、Tool、State、Workflow 与 Orchestration 拆开。'
  }}
>
  <section id="task-first" class="article-section">
    <p class="article-section-label">01 · 先看任务，不背定义</p>
    <h2>“回答我”与“替我完成”之间，差了什么？</h2>
    <p>先假设你给模型一个很普通的任务：</p>
    <blockquote>帮我看看明天北京会不会下雨。如果会下雨，告诉我出门要不要带伞。</blockquote>
    <p>如果这只是一次普通 LLM 调用，它能做的事情其实很有限：读取你给它的上下文，然后生成一段文字。问题在于，<strong>明天真实的天气并不在这段上下文里。</strong></p>
    <p>要真正把这个任务做完，系统至少还得经历几步：</p>

    <figure class="article-flow">
      <div class="article-flow-row">
        <div class="article-flow-node">理解目标</div><span class="article-flow-arrow">→</span>
        <div class="article-flow-node">发现缺少数据</div><span class="article-flow-arrow">→</span>
        <div class="article-flow-node">查询天气</div><span class="article-flow-arrow">→</span>
        <div class="article-flow-node">读取结果</div><span class="article-flow-arrow">→</span>
        <div class="article-flow-node">决定答案</div>
      </div>
      <figcaption class="article-caption">Agent 的变化不是“模型突然知道了天气”，而是系统允许模型参与任务执行。</figcaption>
    </figure>

    <div class="article-key-idea"><strong>关键转折</strong>模型开始从“只负责生成最终答案”，变成“还要判断下一步应该做什么”。</div>
    <p>这就是理解 Agent 最重要的起点。</p>
  </section>

  <section id="boundary" class="article-section">
    <p class="article-section-label">02 · 先把边界分清楚</p>
    <h2>用了 LLM、能聊天、接了 Tool，都不等于 Agent</h2>
    <p>Agent 最容易学乱，是因为几个不同层次的概念经常被混在一起。最简单的区分方法不是看“用了什么技术”，而是问：</p>
    <div class="article-think"><strong>判断问题</strong>这个系统的下一步，主要是谁决定的？</div>

    <div class="article-steps">
      <div class="article-step"><span>LLM</span><div><h3>输入 → 生成</h3><p>模型根据输入和上下文生成输出。它是 Agent 的核心能力之一，但单独的模型不是完整 Agent。</p></div></div>
      <div class="article-step"><span>CHAT</span><div><h3>多轮对话</h3><p>Chatbot 可以保存对话历史，但如果每一轮依旧只是“用户问、模型答”，它仍然主要是一个对话应用。</p></div></div>
      <div class="article-step"><span>FLOW</span><div><h3>代码决定路径</h3><p>Workflow 可以有多个 LLM、多个 Tool，但 A → B → C 的主要路径通常由开发者提前编排。</p></div></div>
      <div class="article-step"><span>AGENT</span><div><h3>模型运行时决定路径</h3><p>模型根据目标和当前反馈，动态决定下一步调用什么工具、是否继续，以及什么时候结束。</p></div></div>
    </div>

    <p>因此，<strong>“给 Chatbot 接一个搜索 API”并不会自动让它变成 Agent。</strong>如果搜索什么时候发生、搜什么、搜完固定进入哪一步，全部由代码写死，它依然更接近 Workflow。</p>

    <div class="article-compare">
      <div><h3>Workflow</h3><ul><li>执行路径主要预先确定</li><li>行为稳定、容易预测</li><li>适合步骤明确的任务</li><li>更容易控制成本和失败边界</li></ul></div>
      <div class="focus"><h3>Agent</h3><ul><li>运行时动态决定下一步</li><li>可以根据反馈改变路径</li><li>适合开放、多步骤任务</li><li>灵活性更高，不确定性也更高</li></ul></div>
    </div>

    <p>Anthropic 对这条边界给出的工程化区分也很清楚：Workflow 的 LLM 与 Tool 由预定义代码路径编排，而 Agent 让 LLM 动态控制自己的执行过程和工具使用。</p>
  </section>

  <section id="definition" class="article-section">
    <p class="article-section-label">03 · 现在再定义 Agent</p>
    <h2>Agent 是一个“围绕目标持续决策和行动”的系统</h2>
    <p>到这里再看定义，会比一开始直接背一句话容易很多。</p>

    <div class="article-key-idea"><strong>本文采用的定义</strong>AI Agent 是一个以模型作为决策核心，能够围绕目标决定下一步行动、调用工具与外部环境交互，并根据执行结果继续调整，直到任务完成或触发停止条件的系统。</div>

    <p>这个定义可以拆成四个词：</p>
    <div class="article-steps">
      <div class="article-step"><span>01</span><div><h3>Goal · 目标</h3><p>系统需要知道最终要完成什么，而不是只处理当前一句话。</p></div></div>
      <div class="article-step"><span>02</span><div><h3>Decision · 决策</h3><p>模型根据当前上下文判断下一步，而不仅仅生成最终文本。</p></div></div>
      <div class="article-step"><span>03</span><div><h3>Action · 行动</h3><p>系统能够查询数据、调用 API、执行代码、修改文件或触发其他外部动作。</p></div></div>
      <div class="article-step"><span>04</span><div><h3>Feedback · 反馈</h3><p>Agent 会读取真实执行结果，再决定任务是否完成、失败，还是需要继续。</p></div></div>
    </div>

    <p>OpenAI 的实践指南把 Agent 的基础设计归纳为三个核心组件：<strong>Model、Tools、Instructions</strong>。这个视角很适合实现阶段；从理解运行机制的角度，还需要再补上“上下文 / 状态”和“循环”。</p>
  </section>

  <section id="loop" class="article-section">
    <p class="article-section-label">04 · Agent 的核心运行机制</p>
    <h2>为什么 Agent 不是一次调用，而是一个 Loop？</h2>
    <p>真实任务不会总在第一步就拥有所有信息。很多时候，系统必须先做一个动作，看到结果后才能决定第二步。</p>
    <div class="article-note"><strong>单次调用</strong>User → LLM → Answer</div>

    <figure class="article-flow">
      <div class="article-flow-row">
        <div class="article-flow-node">Goal / Context</div><span class="article-flow-arrow">→</span>
        <div class="article-flow-node">Decision</div><span class="article-flow-arrow">→</span>
        <div class="article-flow-node">Action</div><span class="article-flow-arrow">→</span>
        <div class="article-flow-node">Observation</div><span class="article-flow-arrow">↺</span>
      </div>
      <figcaption class="article-caption">核心循环：根据当前信息决策 → 执行动作 → 读取真实结果 → 再决策。</figcaption>
    </figure>

    <p>Hugging Face 的 Agents Course 用 <strong>Thought → Action → Observation</strong> 描述这套循环。这里把 Thought 理解成更工程化的 <strong>Decision</strong>：模型根据当前上下文决定下一步应该是什么，而不是要求系统暴露模型内部推理过程。</p>
    <p>Observation 很关键，因为如果 Agent 看不到工具的真实返回，它就无法判断查询是否成功、信息是否足够、是否需要换方案，以及任务是否已经完成。</p>
    <div class="article-key-idea"><strong>Agent 的“智能”很大一部分来自反馈循环</strong>不是一次规划出完美答案，而是不断利用环境反馈修正下一步。</div>
  </section>

  <section id="tools" class="article-section">
    <p class="article-section-label">05 · Tool 是 Agent 与外部世界之间的接口</p>
    <h2>模型负责决定，Tool 负责真正执行</h2>
    <p>LLM 的输出本质上仍然是模型生成的内容。要让 Agent 真正“做事”，就需要把模型能够选择的动作包装成 Tool。</p>

    <div class="article-compare">
      <div><h3>没有 Tool</h3><ul><li>解释天气应该怎么查</li><li>告诉你 SQL 应该怎么写</li><li>生成一封邮件文本</li><li>描述文件应该如何修改</li></ul></div>
      <div class="focus"><h3>有 Tool</h3><ul><li>真正调用天气 API</li><li>真正查询数据库</li><li>真正发送邮件</li><li>真正读取或修改文件</li></ul></div>
    </div>

    <pre class="article-code" data-language="Python"><code>{`def get_weather(city: str, date: str):
    # 调用真实天气服务
    return weather_api.query(city=city, date=date)`}</code></pre>

    <p>真正有价值的不是这几行代码，而是模型能够在合适的时候判断“当前信息不够，我需要调用 get_weather，再根据返回结果继续”。所以 Function Calling / Tool Calling 本身只是能力接口，<strong>Agent 是围绕这些工具组织起来的决策与执行循环。</strong></p>
  </section>

  <section id="when" class="article-section">
    <p class="article-section-label">06 · 不要把所有东西都 Agent 化</p>
    <h2>什么时候 Agent 才真的有价值？</h2>
    <p>Agent 更灵活，但也意味着更多模型调用、更高延迟、更高成本，以及更多不可预测的执行路径。Anthropic 也建议从能解决问题的最简单方案开始，只有当复杂度确实带来收益时才升级到 Agent。</p>

    <div class="article-steps">
      <div class="article-step"><span>TEXT</span><div><h3>一次生成就能完成</h3><p>总结、改写、分类、信息抽取等任务，优先普通 LLM 调用。</p></div></div>
      <div class="article-step"><span>FLOW</span><div><h3>步骤固定、规则明确</h3><p>流程可以稳定写成 A → B → C 时，优先使用 Workflow。</p></div></div>
      <div class="article-step"><span>AGENT</span><div><h3>路径无法提前完全确定</h3><p>需要模型根据中间结果决定下一步，而且步骤数量可能变化时，Agent 才开始体现价值。</p></div></div>
    </div>

    <p>典型场景包括复杂资料调研、Coding Agent、开放式客服和 Computer Use。它们的共同点不是“都用了 LLM”，而是：<strong>执行过程中必须不断读取环境反馈，再决定下一步。</strong></p>
    <div class="article-key-idea"><strong>判断是否需要 Agent</strong>不要问“这个功能能不能用 Agent 做”，而要问“这个任务是不是必须在运行时动态决定下一步”。</div>
  </section>

  <section id="minimal" class="article-section">
    <p class="article-section-label">07 · 把整篇文章压缩成一个最小模型</p>
    <h2>一个 Agent，最小可以理解成什么？</h2>
    <p>如果暂时忽略框架、Memory、Multi-Agent、MCP 等概念，一个最小 Agent 可以压缩成下面这段伪代码：</p>

    <pre class="article-code" data-language="Pseudo Code"><code>{minimalAgent}</code></pre>

    <p>它其实只做三件事：模型根据目标和上下文做 <strong>Decision</strong>；系统执行 <strong>Action</strong>；把工具结果作为 <strong>Observation</strong> 放回上下文，再进入下一轮，直到任务完成。</p>

    <div class="article-key-idea"><strong>第一篇只需要记住一句话</strong>Agent 不是“更聪明的 Chatbot”，而是一个让模型参与任务控制，并通过 Tool 与环境形成反馈循环的系统。</div>

    <div class="article-note"><strong>下一篇再解决</strong>既然核心机制这么简单，为什么还会出现 LangGraph、OpenAI Agents SDK、smolagents 等 Agent Framework？它们到底替我们封装了什么？</div>
  </section>
</LearningArticleLayout>
